{
  "name": "@eonae/common",
  "description": "All sorts of common functions and modules",
  "version": "0.7.2",
  "main": "dist/index",
  "readmeFilename": "README.md",
  "files": [
    "dist",
    "*.md"
  ],
  "scripts": {
    "publish-npm": "npm publish --access public",
    "build": "rm -rf ./dist && npm run compile",
    "clean": "rm -rf ./dist && rm -rf ./node_modules",
    "compile": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "moment": "^2.29.0",
    "moment-range": "^4.0.2",
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "@types/yaml": "^1.9.7"
  }
}
